# nvm

A wrapper around node and related commands that allows for full use without admin and will properly utilise an nvmrc file.
Will allow multiple versions of node to run simultaneously.

## commands:

* list
* use
* install
* uinstall
* run
* init
